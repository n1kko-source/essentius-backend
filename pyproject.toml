[project]
name = "essentius-backend"
version = "0.1.0"
description = "Essentius API — aprendizaje personalizado y autónomo"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
